<?xml version="1.0" encoding="utf-8"?>

<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/home"
        android:title="Home"
        android:icon="@drawable/home_icon"
        app:showAsAction="always">
    </item>

    <item android:id="@+id/appetizers"
        android:title="Appetizers"
        android:icon="@drawable/appetizer_icon"
        app:showAsAction="always|withText">
    </item>

    <item android:id="@+id/main_courses"
        android:icon="@drawable/maincourse_icon"
        android:title="Main"
        app:showAsAction="always|withText">
    </item>

    <item android:id="@+id/desserts"
        android:title="Desserts"
        android:icon="@drawable/dessert_icon"
        app:showAsAction="always|withText">
    </item>

</menu>